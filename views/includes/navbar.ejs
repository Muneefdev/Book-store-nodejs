<nav class="navbar navbar-expand-lg bg-body-tertiary">
   <div class="container-fluid">
      <a class="navbar-brand" href="/">STORE</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
         aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
         <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
         <ul class="navbar-nav">
            <li class="nav-item">
               <a class="nav-link <%= path === '/' ? "active" : "" %>" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
               <a class="nav-link <%= path === '/books' ? "active" : "" %>" href="/books">Books</a>
            </li>
            <% if (isAuthenticated) { %>
            <li class="nav-item">
               <a class="nav-link <%= path === '/user/add-book' ? "active" : "" %>" href="/user/add-book">Add
                  Book</a>
            </li>
            <% } %>
            <% if (!isAuthenticated) { %>
            <li class="nav-item">
               <a class="nav-link <%= path === '/auth/login' ? "active" : "" %>" href="/auth/login">Login</a>
            </li>
            <li class="nav-item">
               <a class="nav-link <%= path === '/auth/signup' ? "active" : "" %>" href="/auth/signup">Signup</a>
            </li>
            <% } %>
            <% if (isAuthenticated) { %>
            <li class="nav-item">
               <a class="nav-link <%= path === '/auth/logout' ? "active" : "" %>" href="/auth/logout">Logout</a>
            </li>
            <% } %>
         </ul>
      </div>
   </div>
</nav>